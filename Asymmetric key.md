One of a pair of keys (i.e. an [[Asymmetric key pair]]) used in [[Asymmetric key Cryptography]]

Each single key within a pair is either known as the [[Public key]] or the [[Private key]], corresponding to whether it is shared (i.e. public) or kept secret (i.e. private).
