A suite of cryptographic algorithms needed to implement a particular security service.

E.g. A system which implements data encryption will have algorithms for (at least) [[encryption]], and [[decryption]].